\begin{schooloriginalmistakes}
\schoolsectionheading{ {{ section.title|default:"Часть 1. Разбор ошибок" }} }
{% for task in payload.tasks %}
\schooltaskheading{ {{ task.order }} }{% if task.group_name %}{ {{ task.group_name }} }{% else %}{}{% endif %}
{% if task.max_points is not None %}
{\small Баллы: {{ task.points }}/{{ task.max_points }}}

{% endif %}
{% include "documents/latex/partials/task_images.tex" with images=task.right_images %}
{{ task.latex_content|default:task.text|safe }}

{% include "documents/latex/partials/task_images.tex" with images=task.bottom_images %}

\textbf{Ответ:} {{ task.answer|safe }}

{% if task.short_solution %}
\textbf{Краткое решение:}

{{ task.short_solution|safe }}

{% endif %}
{% empty %}
\textit{Ошибки не найдены.}
{% endfor %}
\end{schooloriginalmistakes}
